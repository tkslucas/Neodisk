//
//  AppVersion.swift
//  Neodisk
//

/// Bumped on every user-visible change so a running build is identifiable
/// (shown bottom-left in the status bar).
enum AppVersion {
    static let string = "2.52.1"
}
